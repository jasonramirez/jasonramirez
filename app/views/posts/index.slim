section.content.content--reading
  h2
    = t("titles.posts")

  - if @posts.present?
    - @posts.each do |post|
      = link_to post_path(post), class: "post-sample" do
        article
          time
            = post.pretty_published_date
          h4
            = post.title
