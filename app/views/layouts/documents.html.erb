<!DOCTYPE html>
<html lang="en">
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="theme-color" content="#181923">
    <title>Jason Ramirez's Resume</title>
    <style>
      /* Font declarations for documents */
      @font-face {
        font-family: "Charter";
        src: url("<%= asset_path('charter_regular.woff') %>") format("woff");
        font-weight: normal;
        font-style: normal;
      }
      
      @font-face {
        font-family: "Charter";
        src: url("<%= asset_path('charter_italic.woff') %>") format("woff");
        font-weight: normal;
        font-style: italic;
      }
      
      @font-face {
        font-family: "Charter";
        src: url("<%= asset_path('charter_bold.woff') %>") format("woff");
        font-weight: bold;
        font-style: normal;
      }
      
      @font-face {
        font-family: "Passo";
        src: url("<%= asset_path('passo_regular.woff2') %>") format("woff2");
        font-weight: normal;
        font-style: normal;
      }
      
      @font-face {
        font-family: "Passo";
        src: url("<%= asset_path('passo_medium.woff2') %>") format("woff2");
        font-weight: 500;
        font-style: normal;
      }
      
      @font-face {
        font-family: "Passo";
        src: url("<%= asset_path('passo_semi_bold.woff2') %>") format("woff2");
        font-weight: 600;
        font-style: normal;
      }
      
      @font-face {
        font-family: "Passo";
        src: url("<%= asset_path('passo_bold.woff2') %>") format("woff2");
        font-weight: bold;
        font-style: normal;
      }
      
      /* Document-specific styles */
      body {
        font-family: "Charter", 'Helvetica', sans-serif;
      }
      
      label {
        font-family: "Passo", sans-serif;
        font-weight: 400;
        font-size: 8pt;
        color: #999;
      }

      .document {
        font-family: "Charter", 'Times New Roman', serif;
        max-width: 8.5in;
        margin: 0 auto;
        padding: 0.5in;
        line-height: 1.2;
      }

      section {
        margin-top: 36pt;
      }
      
      h1 {
        font-family: "Passo", sans-serif;
        font-weight: bold;
        font-size: 24pt;
        margin-bottom: 0;
        margin-top: 80pt;
      }
      
      h2 {
        font-family: "Passo", sans-serif;
        font-weight: 600;
        font-size: 14pt;
        margin-bottom: 4pt;
        margin-top: 0;
      }
      
      h3 {
        font-family: "Passo", sans-serif;
        font-weight: 500;
        font-size: 12pt;
        margin-top: 0;
        margin-bottom: 4pt;
      }
      
      p {
        margin: 0 0 6pt 0;
      }
      
      .document ul {
        margin: 6pt 0;
        padding-left: 20pt;
        line-height: 1.5;
      }
      
      .document li {
        margin-bottom: 3pt;
      }
      
      .document code {
        font-family: "JetBrains Mono", monospace;
        background-color: #f5f5f5;
        padding: 2pt 4pt;
        border-radius: 2pt;
      }
      
      @media print {
        .document {
          max-width: 600px;
          margin: 0.75in;
          padding: 0;
        }
        
        .document h1 { font-size: 18pt; }
        .document h2 { font-size: 12pt; }
        .document h3 { font-size: 11pt; }
      }
    </style>
  </head>
  <body>
    <div class="document">
      <%= yield %>
    </div>
  </body>
</html>
