<section class="content-full-screen">
  <section class="content">
    <p class="u-margin-bottom-large">
      <a href="/works" class="link">
        <%= image_tag "icon-long-arrow-left.svg", alt: "icon-long-arrow-left" %>
      </a>
    </p>

    <h2>
      <%= t(".heading") %>
    </h2>

    <p> If you'd like a password to view this case study, please contact
      <a href="mailto:jason@jasonramirez.com">jason@jasonramirez.com</a>.
    </p>

    <%= form_for :lockup, url: { action: 'unlock' },
      data: { turbo: false } do |form| %>

      <% unless @wrong %>
        <%= form.password_field "codeword", placeholder: "Password", autofocus:
          true %>
      <% else %>
        <%= form.password_field "codeword", class: "error", placeholder:
          "Password", autofocus: true %>
      <% end %>

      <% if @wrong %>
        <div class="form-error-message">
          Incorrect password. Please try again.
        </div>
      <% end %>

      <% if params[:return_to].present? %>
        <%= form.hidden_field "return_to", value: params[:return_to] %>
      <% elsif @return_to.present? %>
        <%= form.hidden_field "return_to", value: @return_to %>
      <% end %>

      <%= button_tag t(".submit"), class: "button" %>
    <% end %>
  </section>
</section>
